# Fail with a clear error if JDK 17 isn't already installed, instead of silently
# auto-downloading one (which would need a toolchain resolver plugin configured).
org.gradle.java.installations.auto-download=false
